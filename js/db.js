const oracledb = require('oracledb');

const configuracion = {
  user:          "prg2",             // Usuario de Oracle
  password:      "curso2026",        // Contraseña del usuario
  connectString: "localhost/XEPDB1"  // Host / Base de datos
};

module.exports = configuracion;